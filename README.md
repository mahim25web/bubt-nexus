# bubt-nexus

Simple static project ready for GitHub.

## How to push to GitHub

1. Create a repository on GitHub (do not initialize with a README).
2. Add the remote and push:

```
git remote add origin <REMOTE_URL>
git branch -M main
git push -u origin main
```